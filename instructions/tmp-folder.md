# Temporary Files

When you need to write temporary or scratch files, always use `/tmp/opencode/` as the base directory. Never write directly to `/tmp/`. Create subdirectories under `/tmp/opencode/` as needed.
